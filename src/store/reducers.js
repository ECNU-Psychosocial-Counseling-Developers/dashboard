const defaultState = {
  user: {
    username: '',
    role: 'counselor',
  },
};

function reducer(state = defaultState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}

export default reducer;
